let boodschappenlijstje = ['Melk', 'Eieren', 'Suiker', 'Vanille extract', 'Bloem', 'Nutella', 'Roomijs', 'Confituur'];
    
console.log('Mijn boodschappenlijstje:');

for(let i = 0; i < boodschappenlijstje.length; i++){
    console.log(boodschappenlijstje[i]);
}

// .length is een functie die de lengte van de array weergeeft
// als je eronder bevoorbeeld --boodschappenlijst[3] = 'bowl'-- schrijft dan vervang je de waarde die daar origineel stond en ben je de OG waarde dus kwijt